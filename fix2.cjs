const fs = require("fs"); let c = fs.readFileSync("/app/server.js", "utf8"); const old = "PDFDoc.y=rowY+4; PDFDoc.text(cl(it.article_name||it.name),cx+3,rowY+4,{width:TW[1]-6,lineBreak:false,ellipsis:true});"; const rep = "const artName = cl(it.article_name||it.name)+(it.lot_kod?\" | Lot: \"+cl(it.lot_kod):\"\"); PDFDoc.y=rowY+4; PDFDoc.text(artName,cx+3,rowY+4,{width:TW[1]-6,lineBreak:false,ellipsis:true});"; if(c.includes(old)){c=c.replace(old,rep);fs.writeFileSync("/app/server.js",c);console.log("DONE");}else{console.log("NOT FOUND: "+old.slice(0,40));}
